import React, { use } from 'react'

const OrderPage = (
    { params, }: { params: Promise<{ id: string }> }
) => {
    const { id } = use(params)
    return (
        <div>Order</div>
    )
}   


export defaul
{
    {
        params,
}: {
        params: Promise<{ id: string }>
    }t OrderPage: {

    }